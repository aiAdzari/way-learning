/**
 * @param {number[]} nums
 * @return {number}
 */
var maxSubArray = function (nums) {
    for (let i = 1; i <= nums.length; i++) {
        for (let j = 0; j < i; j++) {
            const element = array[j];
            
        }
        
    }
};